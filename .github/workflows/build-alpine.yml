name: build-alpine

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: robusta
        uses: actions/checkout@v6
        with:
          repository: AICafe1/robusta
          token: ${{ secrets.PASS }}
          ref: build24

      - name: data
        uses: actions/checkout@v6
        with:
          repository: AICafe1/robusta-data
          token: ${{ secrets.PASS }}
          path: data

      - name: src
        uses: actions/checkout@v6
        with:
          repository: AICafe1/robusta-src
          token: ${{ secrets.PASS }}
          path: src

      - name: Node.js 24 Alpine Build
        uses: docker://node:24-alpine
        with:
          args: sh -c "npm i && cd data && npm i && cd .. && npm run b-alp && node src/license/private.ts --year=1 LICENSE.key && bin/robustalp abi.js && bin/robustalp --mode=manage && ls -l robusta.sqlite"

      - run: |
          cp bin/robustalp robusta
          chmod +x robusta
          tar -czvf robusta-alpine.tgz robusta

      - uses: actions/upload-artifact@v7
        with:
          name: robusta
          path: robusta

      - name: Release
        uses: softprops/action-gh-release@v3
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: "*.tgz"
